import{angleFraction}from'../utils/fractions.js';import{fractionHtml}from'../utils/formatter.js';export function fractionSteps(angle,step=5){const f=angleFraction(angle);const parts=[`360°のうち${angle}°`,`${angle} ÷ 360`,fractionHtml(angle,360),`上と下を${f.divisor}で割る`,fractionHtml(f.numerator,f.denominator),`円全体の${f.denominator}分の${f.numerator}`];return`<ol>${parts.slice(0,Math.min(parts.length,step+1)).map(x=>`<li>${x}</li>`).join('')}</ol>`}
