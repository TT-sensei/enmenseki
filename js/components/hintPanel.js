export function hintPanel(problem,level=0){return`<section class="panel hint-panel"><div class="toolbar"><div><span class="hint-level">お助け ${level}/5</span><p>${level?problem.hints[level-1]:'答えはすぐに出しません。必要なところまで一段ずつ開こう。'}</p></div><button class="btn btn-secondary" data-next-hint ${level>=5?'disabled':''}>${level?'次のヒント':'ヒントを見る'}</button></div>${level>=3?`<details><summary>3.14計算サポート</summary><p>整数部分を先に計算し、最後に3.14をかけよう。</p><p>3.14倍の例：2倍=6.28、3倍=9.42、4倍=12.56、5倍=15.7、10倍=31.4</p>${level>=4?'<div class="formula-display">整数<br>× 3.14<br>─────<br>小数点の位置をそろえて確認</div>':''}</details>`:''}</section>`}
