import{renderDiagram}from'../diagrams/composite.js';export const diagramViewer=problem=>`<div class="diagram-box" data-diagram>${renderDiagram(problem.diagram)}</div><div class="tag-row" aria-label="分かっていること">${Object.entries(problem.values).filter(([,v])=>typeof v==='number').map(([k,v])=>`<button class="chip" data-known="${k}" aria-pressed="false">${({radius:'半径',diameter:'直径',angle:'中心角',arc:'弧の長さ',side:'正方形の一辺',outerRadius:'外側の半径',innerRadius:'内側の半径',area:'面積',circumference:'円周'}[k]||k)}：${v}</button>`).join('')}</div>`;
