export function confirmModal({title,message,confirmText='実行する',onConfirm}){const root=document.querySelector('#modal-root');root.innerHTML=`<div class="modal-backdrop"><section class="modal" role="dialog" aria-modal="true" aria-labelledby="modal-title"><h2 id="modal-title">${title}</h2><p>${message}</p><div class="actions"><button class="btn btn-danger" data-confirm>${confirmText}</button><button class="btn btn-secondary" data-cancel>キャンセル</button></div></section></div>`;const close=()=>root.replaceChildren();root.querySelector('[data-cancel]').onclick=close;root.querySelector('[data-confirm]').onclick=()=>{onConfirm();close()};root.querySelector('[data-cancel]').focus()}
