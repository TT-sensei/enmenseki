import{loadData}from'../storage.js';import{cardCatalog}from'../services/achievementService.js';
export function collectionView(){const unlocked=loadData().cards;document.querySelector('#app').innerHTML=`<div class="page"><p class="eyebrow">図形図鑑</p><h1>見つけた図形</h1><p class="lead">問題を解くと、図形の見方を記したカードが増えます。</p><div class="card-grid">${Object.entries(cardCatalog).map(([id,c])=>`<article class="card" style="opacity:${unlocked.includes(id)?1:.48}"><div class="mode-icon" aria-hidden="true">${unlocked.includes(id)?'◉':'◌'}</div><h2>${unlocked.includes(id)?c.name:'まだ見つけていない図形'}</h2><p>${unlocked.includes(id)?c.description:'問題に挑戦して見つけよう。'}</p></article>`).join('')}</div></div>`}
